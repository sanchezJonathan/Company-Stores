Order #{{ order.order_id }} @ {{ shop.name }} shipped via {{ shipment.shipment_friendly_name }}. {% if shipment.tracking_number and shipment.tracking_number != empty %}Tracking: {{ shipment.tracking_number }}{% endif %}
