<div>
  <p class="truncated">{{ contact.name }}</p>

  <p class="truncated">{{ address.company }}</p>

  <p class="truncated">{{ address.first_address }}</p>

  <p class="truncated">{{ address.second_address }}</p>

  <p class="truncated">{{ address.city }}, {% if address.state != empty %}{{ address.state }}, {% endif %}{{ address.zip }}</p>

  <p class="truncated">{{ address.country }}</p>

  <p class="truncated">{{ contact.phone }}</p>

  <p class="truncated">{{ contact.email }}</p>
</div>
